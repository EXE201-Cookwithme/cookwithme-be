export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum UserPlan {
  FREE = 'free',
  PRO = 'pro',
}

export enum RoleChat {
  AI = 'ai',
  USER = 'user',
}

export const LLM = 'gpt-4o';
